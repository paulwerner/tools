# Learning 001 — TypeScript `forceConsistentCasingInFileNames`

The correct TypeScript compiler option is `forceConsistentCasingInFileNames`, not `forceConsistentCasingInImports`. The latter does not exist and causes `svelte-check` and `tsc` to error with "Unknown compiler option."

See session 001.
